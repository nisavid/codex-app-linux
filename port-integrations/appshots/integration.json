{
  "id": "appshots",
  "title": "AppShots port integration",
  "description": "AppShots port integration that captures and crops the focused window without core AppShots backend changes.",
  "defaultEnabled": true,
  "entrypoints": {
    "patchDescriptors": "./patch.js"
  },
  "resources": [
    {
      "source": "bin/bare-modifier-monitor",
      "target": "resources/native/bare-modifier-monitor",
      "mode": "0755"
    }
  ]
}
