{
  "id": "appshots",
  "title": "AppShots port integration",
  "description": "Opt-in AppShots port integration that captures and crops the focused window without core AppShots backend changes.",
  "defaultEnabled": false,
  "entrypoints": {
    "patchDescriptors": "./patch.js"
  },
  "resources": [
    {
      "source": "bin/bare-modifier-monitor",
      "target": "resources/native/bare-modifier-monitor",
      "mode": "0755"
    }
  ]
}
