# 2026-06-13 Sync Ledger

## Scope

- PR: pending.
- Merge commit: pending.
- Origin base before sync: `3fe582d8e39abb1eca9da9b3f760b968e9fc6d47`
- Previous divergence baseline:
  `60c62e38bbd12a5d5ddd4e2bf1d513ff4892fe94`
- Synced Linux-port upstream commit:
  `1d2bd267ef9a41ae6be5a604150b5c4ef638d984`

## Incoming Catalog

### Patch System And Official DMG Drift

- Tightens patch report policy so launch-critical patches fail the build while
  noncritical patches remain fail-soft.
- Splits main-process patch code by concern behind the existing barrel module.
- Adds strategy telemetry, current-DMG patch needles, Browser Use webview
  availability/navigation patches, profile settings menu and i18n gate patches,
  and current composer footer patch handling.
- Drops stale pre-current fallback needles and the never-shipped Chrome sync
  filter migration entry.
- Makes patch-report validation, workflow artifact upload, and CI header
  parsing more robust against current runner and toolchain behavior.

### Browser, Chrome, And Window Behavior

- Preserves Browser Use and external Browser Use gates on Linux, including route
  liveness fallback handling.
- Preserves Linux Chrome plugin availability, prefers active Linux Chrome
  profiles, closes discarded profile backends, and keeps CDP access available.
- Adds the native frameless titlebar overlay, repaint hooks, opaque Linux window
  surfaces, menu idempotence, and titlebar overlay fixes.
- Improves avatar overlay bounds and About/build-information dialog handling.

### Port Integrations

- Adds the frameless-titlebar integration.
- Adds the optional X11 EWMH Computer Use integration.
- Preserves enabled local integrations during wrapper updates and user-local
  installs.
- Repairs read-aloud settings patches, espeak voice precedence, and first-PCM
  chunk handling.
- Fixes remote mobile control device key aliases, feature keys, and CLI symlink
  cleanup.

### Updater, Installer, And Launcher

- Adds same-DMG update recovery so already-installed DMG candidates are cleared
  without disrupting ready wrapper package installs.
- Handles missing graphical polkit authentication agents by preserving manual
  install state and reporting the manual install command.
- Completes the Sparkle autodownload stub expected by the official app bundle.
- Restores terminal CLI install prompts, surfaces curl diagnostics, resolves
  cached DMG paths consistently, and ignores space-joined Electron helper names.
- Keeps official-DMG download cache invalidation and portable CI header parsing.

### Nix And Native Build Metadata

- Refreshes Nix pins through official app version `26.609.30741`.
- Adds Nix `cliPackage` wrapping so the launcher can receive a baked
  `CODEX_CLI_PATH`.
- Keeps build information visible in settings and avoids helper variable
  shadowing.

### Docs And Tests

- Upstream simplified its README into a platform install guide and added
  architecture, Nix, native setup, packaging, troubleshooting, updater, and
  Linux Computer Use docs.
- Upstream updated its maintainer guide for the Linux-port repository.
- Test coverage expands around patch drift, Browser Use, read-aloud, titlebar,
  local feature preservation, updater manual-auth handling, and same-DMG
  recovery.

## Local Reconciliation

- Upstream `linux-features/*` changes were ported to
  `port-integrations/*` with `integration.json` manifests and
  `port-integrations` helper APIs.
- Upstream `.github/workflows/upstream-build-app.yml` changes were ported to
  `.github/workflows/official-dmg-build-app.yml`; the upstream-named workflow
  file remains absent.
- The official-DMG patch policy token remains `required-official-dmg`; legacy
  `required-upstream` parsing remains only as compatibility behavior.
- Local product, launcher, package, updater, service, and state names remain
  `codex-app` and `codex-app-updater`.
- The XDG/FHS install layout, native package support roots, unprivileged
  updater boundary, and official app bundle version contract are preserved.
- Upstream user-local install changes were adapted under the local
  `codex-app` paths and integration terminology.
- Incoming root docs were not added. Their user-facing content is owned here by
  this fork's README, usage docs, maintainer docs, and divergence inventory,
  where local names, package layout, and updater policy are explicit.

## Special Handling And Follow-Up

- The new frameless-titlebar integration is imported as an opt-in port
  integration. No separate follow-up is warranted unless build validation shows
  official bundle drift beyond the imported tests.
- The X11 EWMH Computer Use integration remains optional. No new issue is
  warranted from the sync itself; live X11 behavior should be validated when a
  task depends on that integration.
- Upstream docs additions were classified rather than copied. No new issue is
  warranted because this fork's durable docs already own local install,
  packaging, updater, and divergence policy surfaces.
- Same-DMG updater recovery must preserve ready wrapper update packages. The
  imported upstream regression test was adapted to the local package name.
- Legacy `UPSTREAM_DMG_*`, `CODEX_UPSTREAM_DMG_URL`, `linux-features`, and
  `feature.json` aliases remain compatibility surfaces only.

## Verification

- `env CODEX_PATCH_REPORT_JSON=/tmp/codex-sync-2026-06-13-patch-report.json make build-app-fresh`
  refreshed `Codex.dmg` from the official OpenAI app bundle source before
  build validation. The first post-refresh run exposed a local patch-summary
  schema mismatch in `scripts/lib/asar-patch.sh`, which was fixed in this sync.
- `stat -c '%Y %y %n' Codex.dmg` confirmed the refreshed cached DMG timestamp:
  `1781325934 2026-06-13 00:45:34.684470842 -0400 Codex.dmg`.
- `env CODEX_PATCH_REPORT_JSON=/tmp/codex-sync-2026-06-13-patch-report.json make build-app`
  passed using the refreshed cached DMG and generated app version
  `26.609.41114`.
- `node scripts/ci/validate-patch-report.js codex-app/.codex-linux/patch-report.json --profile official-dmg-build`
  passed required patch validation. Optional patch drift remains non-failing and
  is recorded in the generated patch report.
- `node --check scripts/patch-linux-window-ui.js`
- `node --check scripts/patches/registry.js`
- `node --check scripts/patches/main-process/window.js`
- `node --check scripts/patches/launch-actions.js`
- `node --test scripts/patch-linux-window-ui.test.js`
- `env -u 'BASH_FUNC_ml%%' -u 'BASH_FUNC_module%%' bash tests/scripts_smoke.sh`
- `bash -n tests/scripts_smoke.sh`
- `bash -n contrib/user-local-install/files/share/common.sh`
- `bash -n scripts/lib/asar-patch.sh`
- `cargo fmt --check`
- `cargo check -p codex-app-updater`
- `cargo test -p codex-app-updater same_dmg`
- `cargo test -p codex-app-updater manual_install`
- `cargo test -p codex-app-updater wrapper_apply`
- `git diff --check`
- `rg -n "codex-update-manager|linux-features|feature\\.json|UPSTREAM_DMG_" AGENTS.md README.md CHANGELOG.md docs scripts packaging port-integrations contrib tests updater .github`
  found only expected compatibility, test, and maintainer-doc references.
