# 2026-06-19 Sync Ledger

## Scope

- PR: this sync PR
- Merge commit: this commit
- Origin base before sync: `19b21742f5a51f4a5f9ea1b2d8d1dfab4a21ad4c`
- Previous divergence baseline:
  `d260319464d9d8aeb555212834a9fef9586fb89c`
- Synced Linux-port upstream commit:
  `933413a679df51886ea05d1d0eeb8057e44bf764`

## Incoming Catalog

### Linux Desktop Settings Row

- Detects the current SettingsRow export shape before generating the Linux
  desktop settings route.
- Keeps the build source commit row linkable when a commit URL is available.

### Launch Action Socket

- Keeps fresh builds from relying on shadow-prone imported module aliases when
  starting the launch-action socket.
- Makes the launcher wait for an `ok` acknowledgement from the warm-start IPC
  server before treating handoff as successful.

## Local Reconciliation

- Local product, launcher, package, updater, service, and state names remain
  `codex-app` and `codex-app-updater`.
- The SettingsRow export detection was imported while preserving this fork's
  `officialDmg` metadata, `Enabled port integrations` wording, and local DMG
  terminology.
- The launch-action socket fix was imported under the local
  `CODEX_APP_LAUNCH_ACTION_SOCKET` environment variable while preserving the
  legacy `CODEX_DESKTOP_LAUNCH_ACTION_SOCKET` fallback.
- The launch-action socket now uses collision-safe runtime `require(...)`
  calls inside the injected bundle, preserving local `codex-app` socket path
  defaults.
- The launcher warm-start client now waits for the Electron-side
  acknowledgement before exiting successfully.
- No upstream user-facing docs changed in this synced range.
- No renamed-path map entries changed in this synced range.
- The divergence baseline was updated to
  `933413a679df51886ea05d1d0eeb8057e44bf764`.

## Docs Review

- Upstream README had no changes in the synced range.
- Local README and usage docs need no adaptation for this delta.

## Divergence Review

- 1. Local product and package identity: preserved.
- 2. Linux filesystem layout and package payload contract: preserved.
- 3. Package versioning from the OpenAI DMG bundle: preserved.
- 4. Package builder hardening: not affected.
- 5. Updater privilege boundary and install hardening: not affected.
- 6. Updater persisted state and recovery: not affected.
- 7. Launcher and runtime discovery: preserved with the socket acknowledgement
  and collision-safe injected module lookup.
- 8. ASAR patch registry and Linux window behavior: preserved with SettingsRow
  export detection and local build metadata terminology.
- 9. Webview server and Browser Use behavior: not affected.
- 10. Linux Computer Use and bundled plugin packaging: not affected.
- 11. Official DMG source, cache, and Nix metadata: not affected.
- 12. User-local install experiment: not affected.
- 13. Documentation and upstream credit: preserved.
- 14. Security review and hardening backlog: no new security policy gap found.

## Special Handling And Follow-Up

- The refreshed official DMG moved the subagent metadata source helper from the
  app-server manager signals bundle to the host config bundle. The required
  metadata patch now targets both current bundle families and accepts the
  current source shape.
- The refreshed official DMG changed the tray factory setup and icon fallback
  shape. The required tray patch now recognizes the current `appBrand` setup
  call and Linux fallback branch.
- No reusable policy gap was found during this sync.
- No new issue is warranted for this delta.

## Verification

- `node --check scripts/patches/keybinds-settings.js`: passed.
- `node --check scripts/patches/launch-actions.js`: passed.
- `bash -n launcher/start.sh.template tests/scripts_smoke.sh`: passed.
- `git diff --check --cached`: passed before the required patcher drift fixes.
- `node --test scripts/patch-linux-window-ui.test.js`: passed, 279 tests before
  the required patcher drift fixes.
- `env -u 'BASH_FUNC_ml%%' -u 'BASH_FUNC_module%%' bash tests/scripts_smoke.sh`:
  passed before the required patcher drift fixes.
- `env -u 'BASH_FUNC_ml%%' -u 'BASH_FUNC_module%%' npm_config_cache=/tmp/npm-cache npm_config_logs_dir=/tmp/npm-logs make build-app-fresh`:
  refreshed the official DMG cache and failed on required ASAR patch drift in
  `linux-tray`, `main-process-ui`, and `subagent-nickname-metadata-shape`.
- `stat -c '%Y %y %s %n' Codex.dmg`: confirmed the cached DMG was refreshed at
  `2026-06-19 04:28:22.884835087 -0400`, size `519126133`.
- `node --check scripts/patches/main-process/tray.js`: passed after the
  required patcher drift fixes.
- `node --check scripts/patches/webview-assets.js`: passed after the required
  patcher drift fixes.
- `node --check scripts/patch-linux-window-ui.test.js`: passed after the
  required patcher drift fixes.
- `node --check scripts/patches/core/all-linux/webview/subagent-metadata/patch.js`:
  passed after the required patcher drift fixes.
- `node --test scripts/patch-linux-window-ui.test.js`: passed, 281 tests after
  the required patcher drift fixes.
- `env -u 'BASH_FUNC_ml%%' -u 'BASH_FUNC_module%%' npm_config_cache=/tmp/npm-cache npm_config_logs_dir=/tmp/npm-logs make build-app`:
  passed against the refreshed cached DMG.
- `node scripts/ci/validate-patch-report.js codex-app/.codex-linux/patch-report.json --profile official-dmg-build`:
  passed required patch validation; optional patch drift remains non-failing.
- `git diff --check`: passed.
- `bash -n launcher/start.sh.template tests/scripts_smoke.sh`: passed after the
  required patcher drift fixes.
- `env -u 'BASH_FUNC_ml%%' -u 'BASH_FUNC_module%%' bash tests/scripts_smoke.sh`:
  passed after the required patcher drift fixes.
- Review follow-up:
  `node --check scripts/patches/keybinds-settings.js` passed after addressing
  review comments.
- Review follow-up:
  `node --check scripts/patch-linux-window-ui.test.js` passed after addressing
  review comments.
- Review follow-up:
  `bash -n launcher/start.sh.template tests/scripts_smoke.sh` passed after
  addressing review comments.
- Review follow-up: `git diff --check` passed after addressing review comments.
- Review follow-up: `node --test scripts/patch-linux-window-ui.test.js` passed,
  281 tests.
- Review follow-up:
  `env -u 'BASH_FUNC_ml%%' -u 'BASH_FUNC_module%%' bash tests/scripts_smoke.sh`
  passed.
- Review follow-up:
  `env -u 'BASH_FUNC_ml%%' -u 'BASH_FUNC_module%%' npm_config_cache=/tmp/npm-cache npm_config_logs_dir=/tmp/npm-logs make build-app`
  passed against the refreshed cached DMG.
- Review follow-up:
  `node scripts/ci/validate-patch-report.js codex-app/.codex-linux/patch-report.json --profile official-dmg-build`
  passed required patch validation; optional patch drift remains non-failing.
