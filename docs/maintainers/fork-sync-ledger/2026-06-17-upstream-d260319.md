# 2026-06-17 Sync Ledger

## Scope

- PR: this sync PR
- Merge commit: this commit
- Origin base before sync: `e5b0ec9ff070ac6c920adef0b08f54a19915a676`
- Previous divergence baseline:
  `1d2bd267ef9a41ae6be5a604150b5c4ef638d984`
- Synced Linux-port upstream commit:
  `d260319464d9d8aeb555212834a9fef9586fb89c`

## Incoming Catalog

### Nix And Official DMG Pins

- Refreshes upstream Nix pins through official app version `26.611.62324`.
- Keeps native module and app hash metadata current for the refreshed official
  app bundle.

### Launcher, Runtime, And Wrapper Updates

- Prevents duplicate app instances with stronger launcher detection and a
  bootstrap single-instance lock.
- Hardens launcher startup around current issue regressions and marketplace
  sync logging.
- Adds wrapper-updater timeout handling before Electron launch.
- Keeps current computer-use settings and XDG documents paths aligned with the
  official app bundle's current code shape.

### Port Integrations

- Adds the optional node REPL reaper integration to clean leaked Browser Use
  helper processes after a session exits.
- Updates AppShots availability and Wayland shortcut handling, including
  portal feature flags and distinct left/right modifier keycodes.
- Adds Chrome Beta support to the Thorium/Chrome plugin patch path.

### Patch Drift And Webview Behavior

- Updates patch needles for current official app bundle drift, including
  critical-patch failures, avatar overlay focus, projectless documents, chat
  search hydration, and app-server backfill wait behavior.
- Scopes avatar overlay matching to the overlay class and keeps titlebar/window
  patch behavior compatible with the current bundle.
- Adds targeted tests for Chrome plugin patching, Browser Use availability,
  main-process browser matching, and webview patch drift.

### Docs And Tests

- Adds transparent/dark sidebar troubleshooting guidance upstream.
- Expands shell smoke coverage for launcher, Chrome plugin, port integration,
  and current bundle patch behavior.

## Local Reconciliation

- Local product, launcher, package, updater, service, and state names remain
  `codex-app` and `codex-app-updater`.
- `packaging/linux/control` kept the local package identity and local
  dependency ordering while preserving the relevant upstream `libasound2t64`
  ordering behavior already represented in local package metadata.
- Upstream `docs/troubleshooting.md` changes were ported to
  `docs/usage/troubleshooting.md` with local `codex-app-updater` commands and
  XDG paths.
- Upstream `linux-features/appshots/*` changes were ported to
  `port-integrations/appshots/*`, including `electron-args` and
  `integration.json` runtime hook configuration.
- Upstream `linux-features/codex-wrapper-updater/apply-pending.sh` changes were
  ported to `port-integrations/codex-wrapper-updater/apply-pending.sh` while
  preserving local package/updater gating.
- Upstream `linux-features/node-repl-reaper/*` was imported as
  `port-integrations/node-repl-reaper/*` with an `integration.json` manifest
  and hook descriptors instead of the upstream `feature.json` and `stage.sh`
  shape.
- Upstream `linux-features/thorium-chrome-plugin/*` changes were ported to
  `port-integrations/thorium-chrome-plugin/*`.
- Upstream user-local install changes were reconciled into
  `contrib/user-local-install/files/share/common.sh` under the local layout.
- `CHANGELOG.md` keeps the upstream behavior notes but translates the manual
  updater command to `/usr/bin/codex-app-updater`.
- `port-integrations/README.md` now lists `node-repl-reaper` as an optional
  integration example so the imported module is discoverable under current
  terminology.
- The divergence baseline was updated to
  `d260319464d9d8aeb555212834a9fef9586fb89c`.

## Docs Review

- Upstream README had no changes in the synced range.
- Upstream `AGENTS.md` changes are upstream-agent policy and are intentionally
  omitted from this fork's agent policy except where already represented by the
  fork sync policy.
- Upstream `CHANGELOG.md` additions were adapted in this fork's changelog under
  local names.
- Upstream transparent sidebar troubleshooting was adapted into this fork's
  usage troubleshooting page.

## Divergence Review

- 1. Local product and package identity: preserved.
- 2. Linux filesystem layout and package payload contract: preserved.
- 3. Package versioning from the OpenAI DMG bundle: preserved; Nix app version
  pin refreshed.
- 4. Package builder hardening: preserved.
- 5. Updater privilege boundary and install hardening: preserved.
- 6. Updater persisted state and recovery: preserved; wrapper prelaunch timeout
  behavior ported.
- 7. Launcher and runtime discovery: preserved; upstream launcher hardening
  adapted under local app identity.
- 8. ASAR patch registry and Linux window behavior: preserved with current
  official bundle patch drift updates.
- 9. Webview server and Browser Use behavior: preserved with current webview
  patch updates.
- 10. Linux Computer Use and bundled plugin packaging: preserved with current
  settings and Chrome plugin patch updates.
- 11. Official DMG source, cache, and Nix metadata: preserved with refreshed
  pins.
- 12. User-local install experiment: preserved with upstream icon and metadata
  behavior adapted to local paths.
- 13. Documentation and upstream credit: preserved.
- 14. Security review and hardening backlog: no new security policy gap found.

## Special Handling And Follow-Up

- The node REPL reaper remains optional. No new issue is warranted unless live
  Browser Use process cleanup shows integration drift beyond the imported tests.
- The new AppShots Electron args are build-time runtime hooks under
  `port-integrations/appshots/electron-args`; no package follow-up is warranted.
- The upstream transparent sidebar troubleshooting entry is now covered in this
  fork's usage troubleshooting page. No new issue is warranted.
- No reusable policy gap was found during this sync.

## Verification

- Passed: `bash -n launcher/start.sh.template
  port-integrations/codex-wrapper-updater/apply-pending.sh
  port-integrations/node-repl-reaper/after-exit-hook.sh
  port-integrations/node-repl-reaper/cold-start-hook.sh
  port-integrations/node-repl-reaper/reaper.sh tests/scripts_smoke.sh
  contrib/user-local-install/files/share/common.sh`.
- Passed: `node --check scripts/patches/launch-actions.js`.
- Passed: `node --check scripts/patches/main-process/browser.js`.
- Passed: `node --check scripts/patches/webview-assets.js`.
- Passed: `node --check
  port-integrations/thorium-chrome-plugin/patch-chrome-plugin.js`.
- Passed: `node --test port-integrations/appshots/test.js
  port-integrations/codex-wrapper-updater/test.js
  port-integrations/node-repl-reaper/test.js
  port-integrations/thorium-chrome-plugin/test.js
  scripts/lib/patch-chrome-plugin.test.js
  scripts/patches/main-process/browser.test.js
  scripts/patch-linux-window-ui.test.js` (`317` tests passed).
- Passed: `env -u 'BASH_FUNC_ml%%' -u 'BASH_FUNC_module%%' bash
  tests/scripts_smoke.sh`.
- Passed: `git diff --check`.
- Fresh DMG: `stat -c '%Y %y %s %n' Codex.dmg` reported
  `1781746715 2026-06-17 21:38:35.157546451 -0400 486144300 Codex.dmg`.
- Passed local app generation gate:
  `env -u 'BASH_FUNC_ml%%' -u 'BASH_FUNC_module%%'
  npm_config_cache=/tmp/npm-cache npm_config_logs_dir=/tmp/npm-logs
  make build-app-fresh`.
- Passed required patch validation:
  `node scripts/ci/validate-patch-report.js
  codex-app/.codex-linux/patch-report.json --profile official-dmg-build`.
  The report still contains optional patch drift for current bundle needles.
- Passed package builder gate:
  `env -u 'BASH_FUNC_ml%%' -u 'BASH_FUNC_module%%'
  npm_config_cache=/tmp/npm-cache npm_config_logs_dir=/tmp/npm-logs
  make pacman`.
- Package artifact:
  `dist/codex-app-26.611.62324-1-x86_64.pkg.tar.zst`; `.PKGINFO`
  reports `pkgname = codex-app`, `pkgver = 26.611.62324-1`, and
  `arch = x86_64`.
- Package listing check confirmed:
  `/opt/codex-app/.codex-linux/electron-args.d/appshots-electron-args`,
  `/usr/lib/codex-app/update-builder/port-integrations/appshots/electron-args`,
  and `/usr/lib/codex-app/update-builder/port-integrations/node-repl-reaper/*`.
- Debian package build was not available on this host:
  `make deb` stopped after building `codex-app-updater` because `dpkg-deb`
  is missing.
- RPM package build was not available in this sandbox: `make rpm` reached
  `rpmbuild`, but local RPM database and `/var/tmp` writes are blocked.
